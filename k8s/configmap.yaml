apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ai-analyzer
data:
  AI_PROVIDER: "demo"
  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "cicd_analyzer"
  POSTGRES_USER: "analyzer"
  APP_HOST: "0.0.0.0"
  APP_PORT: "8000"
  LOG_LEVEL: "INFO"
